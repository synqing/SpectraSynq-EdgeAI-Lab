---
abstract: "L20 HOST: C0-v2 load_trace already reverted to :c0_hex 32-char / 0.35 s. Helper default 128 unused. No USB."
---

STATUS: PASS (HOST-ONLY. Revert already in source. Cadence CLOSED. No USB.)

CLAIM: The only `:c0_hex` inject path is `load_trace` in `scripts/gate_c0v2_silicon.py`. It already uses **32-char** chunks and **0.35 s** wait. That is the revert from **128-char / ~0.12 s**, which HANDOFF records as `HEX FAIL: parse`. The helper still defaults to 128; this path does not use the default.

EVIDENCE:
- Load path (revert landed): `scripts/gate_c0v2_silicon.py:294–308` `load_trace` → `hx = u16be_hex(samples)` then `for part in chunk_hex(hx, 32):` then `cmd(ser, f":c0_hex={part}", 0.35)`. Sole `:c0_hex=` writer in this repo.
- Wait contract: `cmd` at `:262` (`wait: float = 0.45`); inject overrides to **0.35**, not 0.12. Zero `0.12` in this script.
- Trap default (unused here): `src/edgeai/mir/gate_c0v2.py:464` `def chunk_hex(hexs: str, n_chars: int = 128)`. Only other import is the silicon script, which always passes `32`.
- Scar: `docs/agent/HANDOFF.md:72` `128-char :c0_hex` → `HEX FAIL: parse`; `:91` future inject = hex chunk 32.
- Not this path: `src/edgeai/serial_studio.py:287` leftover `time.sleep(0.12)` is SS shuttle, D19 demoted, does not emit `:c0_hex`. Retired two-clock `scripts/gate_c0_silicon.py` has no `:c0_hex`.
- Tests: `tests/test_gate_c0v2.py` hex-roundtrips `u16be_hex` only; no `chunk_hex` size or wait assertion.

COMMAND: none. HOST grep/read only. Did not open `/dev/cu.usbmodem*`. Did not run `gate_c0v2_silicon.py`. Cadence CLOSED.

METHOD_RISK: HOST-ONLY source contract. Silicon parse not re-proved this lane. A new caller of `chunk_hex(hx)` without `32` re-opens HEX FAIL. 0.12 s is not on the C0-v2 inject path today; do not copy the SS leftover wait onto hex.

NEXT: Keep `chunk_hex(hx, 32)` + `cmd(..., 0.35)` on any future inject. Optional later (not this lane): change helper default to 32 and add a HOST unit test. Do not USB-test. Do not flash.

USB: none. AUDIO: none. SAME_SONG_LOOP: not started.

---
**Document Changelog**
| Date | Author | Change |
|------|--------|--------|
| 2026-08-31 | agent:grok | Created. L20: load_trace 32 not 128. |
| 2026-08-31 | agent:grok | Re-derive: 32-char / 0.35 s revert confirmed; 128 default unused; 0.12 not on load path. |
