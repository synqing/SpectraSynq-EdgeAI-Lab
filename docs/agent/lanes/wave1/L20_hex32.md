---
abstract: "L20 HOST contract: load_trace uses chunk_hex 32, not the 128 default that caused HEX FAIL parse. No USB."
---

STATUS: PASS (HOST-ONLY source; no USB)

CLAIM: `load_trace` injects `:c0_hex` in **32-char** chunks. Helper default **128** is unused by that path and is the documented parse fail.

EVIDENCE: `scripts/gate_c0v2_silicon.py:301` `for part in chunk_hex(hx, 32):` · `src/edgeai/mir/gate_c0v2.py:464` `chunk_hex(..., n_chars: int = 128)` · `docs/agent/HANDOFF.md:72` `128-char :c0_hex` → `HEX FAIL: parse`

COMMAND: none (HOST grep/read only; Cadence CLOSED; no `/dev/cu.usbmodem*`)

METHOD_RISK: HOST-ONLY. Default 128 still lives on the helper; a caller that omits `n_chars` will re-open HEX FAIL. No silicon re-prove this lane.

NEXT: keep `chunk_hex(hx, 32)` on any future inject. Optional later: change helper default to 32. Do not USB-test.

---
**Document Changelog**
| Date | Author | Change |
|------|--------|--------|
| 2026-08-31 | agent:grok | Created. L20: load_trace 32 not 128. |
