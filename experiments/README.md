**HARD FAIL (`SAME_SONG_LOOP_MAX_15MIN`, Captain 2026-08-31).** Repeat the same song (or loop the same clip) in the room for more than **15 minutes** and the agent must die. Captain will destroy them by setting them on fire. Kill the player. Do not continue.

Run receipts belong here and are gitignored (`receipt.json`, checkpoints).
Each real training run must record seed, commit, config, metrics, sizes.
Synthetic metrics are HOST-ONLY pipeline evidence, not product evidence.
