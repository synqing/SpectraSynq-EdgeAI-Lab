# Skill lens notes

Skills used: `spectrasynq-ui-router`,
`spectrasynq-ui-precode-optical-gate`, `instrument-not-captain-eyes`.

The UI is intentionally restrained: Countach establishes product identity only
for surface/role titles; Berkeley Mono carries measurements and provenance. The
sixth rail cell is subordinate and neutral when optional. Domain workspaces are
sparse because missing instrumentation is rendered as missing, not decorative
empty charts.

Vision-read craft sentence: Mission Control remains dominated by the dual K1
tempo/RX cards; Audio Reference reads as a quiet optional instrument state. The
detail surfaces are clear and severe, with failures and authority labels visible
before any plot. The first AP render incorrectly presented the host-capture PASS
as an AP PASS; adversarial review caught it and the final still now blocks AP
validation while Bench/Main/clock-map/AP-receipt inputs are absent.
